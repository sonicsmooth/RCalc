#ifndef RCTYPES_H
#define RCTYPES_H

typedef enum {UNDEFINED, INPUT, OUTPUT} dir;
typedef enum {UNDER, OVER, PROPER, MIXED} constype;
typedef enum {NONE, VTOP, VBOT, VMID, R1, R2, CURR, RATIO} vartype;

#endif
